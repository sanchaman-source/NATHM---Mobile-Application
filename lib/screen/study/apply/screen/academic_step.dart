import 'package:flutter/material.dart';

class AcademicStep extends StatelessWidget {
  const AcademicStep({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}